fullnameOverride: "prometheus-${cluster_name}"

prometheus:
  externalLabels:
    cluster: "${cluster_name}"

  retention: "${retention}"

  thanos:
    image: quay.io/thanos/thanos:v0.32.5

  thanosService:
    enabled: true

grafana:
  enabled: false